# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.hypothesis/
build/
dist/
*.egg-info/
.venv/
venv/

# Application workspace state (Freebuff client) — not project content
.freebuff/

# Generated AF3 job files / export trees (regenerate with `configbuilder`)
# — root-anchored so it never matches the configbuilder/output source package
/output/

# Internal working copies (.cbproj autosave) — the application's resume state
.configbuilder/

# OS and editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Local development material kept out of the repository (still on disk):
# specification/planning documents, the test suite, and its fixtures.
tests/
fixtures/
testdata/
docs/
IMPLEMENTATION_PLAN.md
AUTHORITATIVE_SPEC.md
DOMAIN_MAPPING.md
HANDOFF.md
CHECKLIST.md
